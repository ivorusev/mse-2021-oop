package com.mse.oop.introduction;

/**
 * The Sumator class contains methods for summing numbers.
 * 
 * @author ivo.rusev
 *
 */
public class Sumator {

	/**
	 * Sums two numbers.
	 * 
	 * @param firstNumber  the first number we want to sum
	 * @param secondNumber the second number we want to sum
	 * @return the sum of the two parameters.
	 */
	public static int sum(int firstNumber, int secondNumber) {
		return firstNumber + secondNumber;
	}
	
	/**
	 * ToDo: document me
	 * 
	 * @param firstNumber
	 * @param secondNumber
	 * @return
	 */
	public static float sum(float firstNumber, float secondNumber) {
		return firstNumber + secondNumber;
	}

}
